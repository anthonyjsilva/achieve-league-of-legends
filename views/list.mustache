{{> header}}

<main>
  <div class="splash">
    <div class="list-container">
      <h2>Achievements</h2>
      <div class="medal-list-container">
        {{#medals}}
          <div class="medal">
            <img class="medal-list-img" src="{{image}}" alt="">
            <br>
            <div class="medal-list-title">{{name}}</div>
            <div class="">{{info}}</div>
          </div>
        {{/medals}}
      </div>

    </div>
  </div>
</main>

{{> footer}}
